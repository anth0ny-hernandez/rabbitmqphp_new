[dmzServer]
BROKER_HOST = 172.22.53.55  ; Replace with the actual DMZ server IP address
BROKER_PORT = 5672
USER = test               ; Replace with DMZ-specific username if needed
PASSWORD = test       ; Replace with DMZ-specific password if needed
VHOST = testHost              ; Replace with a dedicated virtual host for DMZ operations if configured
EXCHANGE = dmzExchange       ; Exchange for DMZ requests
QUEUE = dmzQueue             ; Queue for DMZ request processing
AUTO_DELETE = true
